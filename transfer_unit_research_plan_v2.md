# Research Plan v2 — Tasks, Traces, or Spans?
### 定位多任务 agentic RL 中跨任务加速效应的最小承载单元
版本 v2.0(取代 v1) · 目标 ICLR 2028 主会冲 Outstanding · 周期 14 周 · 按 §11 任务序列可直接交给 agent 自动执行

---

## 0. 核心科学问题与"值不值得做"的论证

**核心问题**:当任务 C 的存在加速了无关任务 B 的学习,这个效应的承载单元是什么?四个候选层级:
- **L1 任务级(field 级)**:效应是 C 这个任务族的整体属性(领域、格式、奖励结构),任何实例子集等效。
- **L2 实例级**:只有 C 中某类实例(如会触发报错的实例)携带效应。
- **L3 轨迹级(trace 级)**:同样的任务、同样的实例,只有含特定事件的 rollout(如"报错→恢复"轨迹)进入梯度才有效——注意在 on-policy RL 里训练数据是任务,trace 是策略自己生成的,所以 trace 级问题=**哪些自生成 rollout 进入梯度才起作用**。
- **L4 片段级(span 级)**:效应由 trace 内特定 token 片段上的梯度承载(如"读报错后的第一个动作"片段),mask 掉这些 span 的 loss,效应消失。

**为什么值得做(直接回答你的问题)**:
1. 答案在哪一层,决定实用交付物是什么,且价值逐层跳升:L1 → 一张混合配比表(接近 mixture optimization,故事最弱);L3 → 一个跨任务通用的 **trace 过滤器**(把任何混合里的加速轨迹升权,不再依赖找对任务);L4 → 可以**合成**加速片段数据,连任务都不需要了。
2. 它判别机制理论:L1 支持"领域知识/格式"解释,L3/L4 支持"行为转移"解释——粒度定位实验本身就是机制证明的一半。
3. L3 的干预(保留任务、过滤进入梯度的 rollout)是 **RL 独有的实验能力**——监督学习的数据归因做不了"同数据不同轨迹"的对照,这是方法学上别人没有的杠杆。
4. 已知的最近邻恰好停在门口:单任务推理里已有"有效学习信号集中在少数高熵 token"的 span 级发现,轨迹过滤作为训练技巧存在(success-only RFT、GRPO-RoC),但**没有人把轨迹/片段当作跨任务迁移的归因单元**。
**诚实的风险**:若答案是纯 L1,论文塌向配比优化,靠备胎故事兜底(§8)。整个设计的目的就是高效地把答案逼出来。

**一句话主张(预期主线)**:多任务 agentic RL 的跨任务加速不由任务承载,而由轨迹事件承载——移除任务但保留其加速轨迹,效应保留;保留任务但过滤掉这类轨迹,效应消失;进一步 mask 掉轨迹中的关键片段梯度,效应同样消失。据此我们交付一个任务无关的加速轨迹过滤器与可合成的片段数据配方。

---

## 1. 术语与判据

- **加速对 (C→B)**:满足 v1 四判据(速度非渐近、低占比饱和、早期窗口充分、自身分数解耦)的任务对。本版继承这些判据,但只作为**入口筛选**,主体是粒度定位。
- **trace 特征 schema(每条 rollout 由规则自动打标,零人工)**:
  `success∈{0,1}`;`err_event`(观测含工具报错);`err_recovery`(err_event=1 且最终 success=1);`err_giveup`(err_event=1 且失败);`verify_cnt`(提交前测试/校验动作数);`clarify`(含提问动作);`replan`(失败观测后动作类别切换);`len`(动作数);`adv_mag`(GRPO 优势绝对值);`ent`(平均策略熵);`novelty`(与最近 200 条 rollout 的嵌入最近邻距离)。
- **span 标注器(规则)**:
  `S_err_react`:从报错观测结束到下一动作结束的模型 token;`S_verify`:校验动作 + 其后决策 token;`S_plan`:轨迹首动作前的推理 token;`S_final`:最后一动作。标注器纯规则实现,在 100 条人工标注轨迹上 span F1 ≥ 0.85 为验收线。

---

## 2. 识别方法学:四层各自怎么"找"

**L1 任务级识别(继承 v1,压缩规模)**
- 24 任务池 × 60 个随机子集运行(p=0.5,Qwen3-1.7B,150 步)→ ridge 拟合任务影响矩阵 Θ;split-half r ≥ 0.5 过门。产出 top-3 加速对 (C→B) 进入下层。

**L2 实例级识别**
- 对每个 top 任务 C:按可观测实例特征二分(是否易触发报错:用基线策略 rollout 8 次统计报错率 >30% 为"高触发组"),两半各自替换整任务做 LOO;若两半效应差 ≥ 2× 标准误 → 实例级信号存在,继续用 20 个实例随机子集运行做实例级 datamodel 回归精定位。

**L3 轨迹级识别(本文核心,双通道)**
- **观察通道(免费)**:多任务训练日志里,每个梯度 step 的 batch 天然含不同 trace 特征构成。回归:目标任务 B 在 step t 后 20 步窗口的 rolling pass@1 增量 ~ step t batch 中 C 的各 trace 特征占比(控制:B 自身近期表现、全局熵、步数)。产出特征级"疑似载体"排序。这一步不花额外算力,只花分析。
- **干预通道(定案)**:见 §3 的 E5 轨迹过滤消融。

**L4 片段级识别**
- 观察通道:对疑似载体轨迹,计算各 span 的梯度范数占比与"span 梯度与 B 成功方向的逐层余弦"(每 25 步抽样 1 个 batch 计算,控制开销);排序候选 span。
- 干预通道:E7 的 span 梯度 mask 消融定案。

---

## 3. 消融实验机制规范(每种消融的精确操作,含 GRPO 语义处理)

**A1 任务级消融**:从采样池移除 C,用"中性填充任务"(Θ 行≈0 已验证)补齐 token 与步数。
**A2 实例级消融**:C 保留,实例池换成指定子集(高触发/低触发/随机对照),实例数与采样频率不变。
**A3 轨迹级过滤消融(关键创新,精确定义)**:C 正常采样、正常 rollout(agent 的交互经验完全不变),仅在**梯度计算前**过滤 C 的 rollout:
- 优势处理:先在完整 group(G=8)上计算 GRPO 优势(保证 baseline 无偏),然后仅对保留 rollout 应用梯度;被滤 rollout 的 token 全部 loss-mask。记为 advantage-frozen 方案(主方案)。
- 稳健性副方案:group 粒度过滤(按 group 内多数特征整组保留/丢弃),验证结论不依赖优势处理方式。
- 算力对齐:通过对 C 超采样后再下采样,使各条件进入梯度的 C-token 数完全一致(±2%)。
- 过滤条件集(E5 的自变量):F1 仅成功轨迹;F2 仅失败轨迹;F3 仅 err_recovery 轨迹;F4 仅无报错的成功轨迹;F5 与 F3 等量的随机轨迹;F6 全保留(对照);F0 全过滤(=只交互不学习,检验"经验本身"是否有效应——这是个隐藏彩蛋对照:若 F0 仍有效应,说明加速经由 B 的 rollout 分布而非 C 的梯度,机制图谱大改)。
**A4 span 级 mask 消融**:F6 全保留,但对指定 span 的 token loss-mask;对照=mask 等长度随机 span;算力天然对齐(token 数差异 <1%,记录并报告)。
**A5 离线通道消融(把"trace 是数据"变成字面意义)**:从捐赠运行收集 C 的 2k 条 rollout,按特征筛成平行数据集(同实例、同数量、只有 trace 特征不同),分别 SFT 到新鲜模型(1 epoch,同超参),之后**只在目标任务 B 上做 RL**,比较 time-to-τ。这是最便宜、最字面的"哪些 trace 是催化数据"实验,且完全绕开 on-policy 的复杂性。

---

## 4. 任务池与数据(24 个,较 v1 精简)

- 目标任务(7):SWE-Gym Lite 300 题(过滤 flaky)、Musique 500(本地索引)、ALFWorld、WebShop 500、BFCL 多轮、BIRD-lite、模拟客服。
- 加速候选(12):读报错修一行 / 单测撰写 / diff 审查 / 长观测摘要 / 澄清提问 / 失败重规划 drill / Sokoban-easy / 格式 drill / 文件定位 / 状态回读 / 算术校验 / 中性填充。全部含 build.py+verify.py,合成配方入库。
- 干扰候选(3):题面泄漏检索、可特判代码题、废话容忍任务。
- 技能双标签(领域×行为)由 API 模型标注 + 10% 人工抽查,用于剔除课程对。
- 捐赠 trace 库(供 A5):对每个 top-C,用 4B 基线策略采 2k rollout 并全量打 trace 特征,入库为 parquet。

---

## 5. 基础设施

- 训练:verl,GRPO,group=8,观测 loss-mask,均匀任务采样;1.7B(L1 回归)/4B(L2–L4 与兑现)。
- 日志(每 10 步/每任务):rolling pass@1、优势方差、熵、6 个行为计数器 + **每条 rollout 的完整 trace 特征行**(这是 L3 观察通道的原料,v1 没有,必须从 E0 就开始记)。
- 梯度探针:每 25 步抽 1 batch 计算 span 梯度范数与跨任务余弦,写独立表。
- 全部消融条件用同一配置生成器驱动:`gen_ablation.py --level {task,instance,trace,span,offline} --condition F3 --seed ...`,保证条件间只差目标变量。

---

## 6. 实验清单(E 后括号=在哪一层)

| ID | 实验 | 规模 | 验收/决策门 |
|---|---|---|---|
| E0 | 基础设施 + trace 特征/span 标注器验收(F1≥0.9 / span-F1≥0.85)+ 冒烟 | 5 运行 | 日志完整;同 seed 重放一致 |
| E1 | (L1) 24×60 随机子集回归 → Θ、top-3 加速对 | 60 GPU·日 | **G1**:split-half r≥0.5;≥1 跨技能对 |
| E2 | (L1) top 对 LOO+剂量曲线确认(4B,3 seeds) | 25 GPU·日 | v1 四判据通过 |
| E3 | (L2) 实例二分 LOO;有信号则实例级回归 | 15 GPU·日 | 实例级信号有/无判定 |
| E4 | (L3 观察) 历史日志的 batch 组成回归(纯分析) | 0 | 疑似载体特征排序 |
| E5 | (L3 干预) 轨迹过滤 F0–F6 × top-2 加速对 × 3 seeds | 45 GPU·日 | **G2(全文最重)**:F3 与 F1/F4/F5 的 time-to-τ 差异显著 → trace 级成立;各 F 无差异 → L1 结论 |
| E6 | (L3 离线) A5 平行 SFT 数据集 → RL-on-B | 20 GPU·日 | 与 E5 结论互证(on/off-policy 双通道一致性) |
| E7 | (L4) span mask 消融:S_err_react/S_verify/随机 span 对照 | 20 GPU·日 | **G3**:特定 span mask 使效应消失 → span 级定位 |
| E8 | 混淆电池(继承 v1):熵匹配、格式饱和、点火检查——但施加在 **E5 的 F 条件间**而非任务间 | 20 GPU·日 | 排除"F3 只是熵更高/优势更大":增加 adv_mag 与 ent 匹配的 F5′ 对照 |
| E9 | 兑现:任务无关 trace 过滤器——在全混合上线"err_recovery 与 verify 轨迹升权"的采样/梯度权重,vs 均匀 vs success-only RFT 惯例 | 25 GPU·日 | 等算力下 time-to-τ 与终点均不劣、速度显著优 |
| E10 | 兑现 2(仅当 G3 通过):合成 span 数据(模板生成 5k 条"报错→引用→修复"片段)SFT 打底,替代整个加速任务 | 10 GPU·日 | 合成片段 ≈ 真任务效应的 ≥70% |
| E11 | 稳健性:换 Llama 家族 + 8B 复跑 E5 主对比 | 25 GPU·日 | 结论跨模型 |

统计:time-to-τ 用删失感知生存分析(log-rank);所有主对比 3 seeds + bootstrap CI;E5 的多重比较做 Holm 校正。

---

## 7. 关键设计细节:为什么 E5 能"明确解释"

E5 的七个条件构成一个完备的判别矩阵:
- F3 > F5(等量随机)⇒ 不是"多了些 C 梯度",是**特定轨迹**;
- F3 > F1 且 F3 > F4 ⇒ 不是"成功经验",是**含错误恢复事件的经验**;
- F2 与 F6 对比 ⇒ 失败轨迹单独是否有毒/有效;
- F0 有无效应 ⇒ 区分"梯度通道"与"rollout 分布通道";
- E8 的 F5′(优势幅度与熵都匹配的随机轨迹)⇒ 排除"F3 恰好优势大/熵高"的统计混淆;
- E6 的离线互证 ⇒ 排除 on-policy 特有伪影(优势处理、采样耦合);
- E7 的 span mask ⇒ 把"哪条轨迹"推进到"轨迹里的哪一段梯度"。
七条全走完,"这些 trace 真的在起加速作用"就不再是相关性陈述,而是一个被双通道、三层级、双向干预钉死的因果定位。

---

## 8. 结果决策树与故事版本

- **O-span(G1,G2,G3 全过)**:主线"迁移的最小单元是片段"——图 1 Θ 热图 → 图 2 F 条件对比 → 图 3 span mask → 图 4 合成片段兑现。冲奖配置。
- **O-trace(G2 过,G3 不过)**:主线"迁移的单元是轨迹事件,不可再分"——同样完整,交付 trace 过滤器(E9),仍是强主会论文。
- **O-task(G2 不过:各 F 无差异)**:主张反转为"跨任务加速是任务整体属性,与轨迹内容无关"——这本身是对 success-only RFT 惯例的一个有趣否定(过滤什么都不影响迁移),配 Θ 仪器发表;中档。
- **O-null(G1 不过)**:方法学论文(RL 噪声下运行级归因的极限)或终止。
- 每个 O 出口在 reports/ 里预置了论文骨架模板,agent 按判定自动选用。

---

## 9. 算力与时间线

总算力 ≈ 270 GPU·日(8×H100 约 7 周训练)。周 1–2:E0+Task 0 查新(词表:"trajectory-level attribution cross-task RL"、"rollout filtering transfer LLM agent"、"span-level credit multi-task RL"、"which trajectories matter GRPO");周 3–5:E1/E2;周 5–6:E3/E4;周 6–8:E5(最优先保 seeds);周 8–9:E6/E7;周 9–11:E8/E9;周 11–12:E10/E11;周 13–14:写作。

---

## 10. 审稿人攻击与预案

| 攻击 | 预案 |
|---|---|
| "轨迹过滤改变了优势估计,是训练伪影" | advantage-frozen 主方案 + group 粒度副方案双轨报告;E6 离线通道与 on-policy 结论互证 |
| "F3 轨迹就是优势更大/熵更高" | E8 的 F5′ 匹配对照专门杀这条 |
| "span 级早有'高熵 token 驱动 RLVR'" | 明确差异:该发现是单任务内、熵定义的 token;我们是跨任务迁移、行为定义的 span,且 E7 加"高熵等量 token mask"对照直接判别两者 |
| "success-only RFT 大家都在用,你只是说它有时错" | 这正是卖点之一:若 O-trace 成立,惯例性的成功过滤在系统性丢弃迁移载体——给出量化代价 |
| "只是课程/难度效应" | 技能标签剔除课程对 + E5 在同一任务内比较(难度分布恒定,课程解释被构造性排除) |
| "规则标注器不可靠" | F1/span-F1 验收线 + 全量开源 + 对标注扰动的敏感性分析 |

---

## 11. 仓库结构与 agent 任务序列

```
transfer-unit/
  tasks/                      # 24 任务族 build/verify/meta
  training/gen_ablation.py    # 统一条件生成器(level×condition×seed)
  tracing/features.py         # rollout 特征行;tracing/spans.py span 标注器(含单测与人工标注对齐脚本)
  grad_probe/                 # span 梯度范数与跨任务余弦探针
  datamodel/                  # L1 回归;analysis/batch_reg.py(L3 观察通道)
  offline/                    # 捐赠 trace 库、平行 SFT 数据集构建
  filterlib/                  # E9 的任务无关 trace 过滤器(交付物)
  synth/                      # E10 片段合成模板
  reports/                    # 模板含 O-span/O-trace/O-task/O-null 四套论文骨架
```
执行序:T0 查新 → T1 任务池 → T2 特征与 span 标注器(过验收线才放行) → T3 训练与日志 → T4 E1+G1 → T5 E2 → T6 E3/E4 → T7 E5+G2(**里程碑,出结果即挂 arXiv 短版**) → T8 E6 → T9 E7+G3 → T10 E8 → T11 E9/E10 → T12 E11 → T13 图表与按 O 出口选骨架成稿。每个 T 产出 reports/<T>.md 并附验收判定。

---

## 12. 风险登记表

| 风险 | 概率 | 缓解 |
|---|---|---|
| E5 各条件差异被 seed 方差淹没 | 中 | 优先给 E5 加 seeds(砍 E11 预算);泛函用 AUC;生存分析提功效 |
| err_recovery 轨迹天然稀少,F3 条件采不满 | 中 | 对 C 超采样(§3 已内置);实例池偏向高触发实例(E3 的副产品) |
| span 梯度探针开销过大 | 低 | 25 步抽样 + 只对 top 对开启 |
| 结论是 O-task(故事最弱) | 中 | 备胎骨架预置;O-task 对 RFT 惯例仍有一个可发表的否定性含义 |
| 被抢发 | 低–中 | T0 查新 + E5 出结果即占位 |
