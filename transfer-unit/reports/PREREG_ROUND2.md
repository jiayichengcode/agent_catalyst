# 预注册预测 第二轮(2026-08-29,五个远场领域,训练前落盘)

## 判据 v4(第一轮修正后:cos 窗口被证伪,仅保留秩条件)
  加速(≥2.5×) ⟺ rank_gain = eff_rank(B∪C) − eff_rank(B) > 0
第一轮记录:2 胜(sql 5.8×, pyfix 7.2×) 1 负(codegen 自身 4.1×,cos 窗口条件失效);
neutral(rank_gain −7.08)是唯一的 2.4× 慢速掺料 → 秩条件仍站立。

## 待测领域(与 codegen 形态迥异,全部单轮无工具)
logic_grid(符号约束推理) / story_qa(叙事多跳) / table_reason(表格数值) /
translate(纯语言映射,零推理——最极端对照) / spec_write(结构化生成)

## 预测规则(几何测完后逐个填写,不得在看到训练结果后改动)
rank_gain > 0  → 预测 tt@0.7 < 6,000(≥2.5×)
rank_gain ≤ 0  → 预测 tt@0.7 > 9,000(<1.7×)

## 锁定的预测(几何已测,训练未跑)
| 领域 | rank_gain | cos_within_C | 预测 |
|---|---|---|---|
| logic_grid | +7.22 | 0.008 | **FAST** tt@0.7 < 6,000 |
| table_reason | +5.37 | -0.005 | **FAST** < 6,000 |
| spec_write | +1.60 | 0.128 | **FAST(边缘)** < 6,000 |
| story_qa | **-7.35** | **0.913** | **SLOW** > 9,000 |
| translate | **-5.67** | 0.506 | **SLOW** > 9,000 |
基准:none 15,215;已知快速掺料 1,768-5,241;已知慢速掺料 neutral 6,305 / b90k 9,035。
落盘时间:训练启动前。
