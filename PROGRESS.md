# 项目进度 · agent_catalyst

**快照日期:2026-08-31** · Qwen3.5-4B · 157 个 run · 11 个合成任务 · 8–16× H100

> 本文件是项目的**当前状态总览**。逐条实验的完整实验室记录见
> [`transfer-unit/reports/HANDOFF.html`](transfer-unit/reports/HANDOFF.html)(2026-08-29 快照)。
> 本文件在其基础上补入 Round 3 追加 seed 后的复算结果。

---

## 1. 一句话状态

**主假设已被自己的对照实验否证,项目处于"负结果已定案、待选择下一步方向"的节点。**

在当前设定下,"催化剂数据"**没有通过预算对照**:表现最好的辅助数据把目标任务
相对欠训练基线加速 ~2.4×,但**补充等量目标任务自己的数据能得到完全相同的曲线**
(AUC 0.719 vs 0.710,n=8/8,p=0.88)。按**总 token** 口径,辅助条件甚至没有优势。

这不是"辅助数据没用",而是**"它的用处等同于多训一会儿"**。
这是一个提法良好的问题得到的真实答案,而负结果之所以有意义,完全来自为它建起来的那套对照。

**否证是有范围的**:单一目标任务、单一模型规模(4B)、单一数据规模区间。
仍然开放的方向见 §7。

---

## 2. 被测的想法

化学催化剂有三个特征:**亚化学计量**、**不被消耗**、**改变速率而不改变平衡**。
假设是某些训练数据也这样——少量掺入目标任务数据中,目标任务收敛显著加快
(经典说法:math 数据提升 coding 能力)。

两个推论决定了整个项目的形态:
1. **因变量必须是速度**——达到质量线所需的目标任务 token 数(time-to-τ),而非终点质量。
2. **交付物不只是现象,而是一个能在训练前预测"哪些数据会加速哪个任务"的理论。**

---

## 3. 五个立场的生死(按时间顺序)

每一个都是**被对照实验杀死的,不是被观点杀死的**。这是新接手的人避免重跑死路的最快路径。

| # | 立场 | 状态 | 致命对照 |
|---|---|---|---|
| **H1** | 跨任务加速由 RL 中特定 rollout 承载 | **否证** | 先是采样 bug(见 §6),修复后 F0–F6 各过滤条件曲线完全重合。结构性原因:GRPO 在任务自己的 group 内算优势,辅助数据没有进入 B 学习信号的通路;on-policy 数据本身已多样。顺序课程是纯税(B 晚点火 ~20 步)。 |
| **H2** | SFT 中掺辅助轨迹能提升终点质量 | **否证** | 预算对照。30k 单独 0.583,加 230k 辅助到 0.896 看似很强;但同样 30k 训 3 epoch 到 0.778、90k 新目标数据到 0.806——"催化"主要是**欠训练被修好了**。 |
| **H3** | 有效成分是错误恢复轨迹 | **否证** | 合作者自己的反驳:错误恢复是**目标数据自身的主导模式**,模型只是学了同一分布更宽的一片。是数据质量发现,不是催化——催化剂**被消耗了**(辅助任务分数同步上升),且需要**化学计量**的量才起效。 |
| **H4** | 加速可由梯度几何预测(秩补充假说) | **否证** | 自己的 out-of-sample 检验。五个远场领域判据 **1/5 命中**:logic 预测最快却垫底(9,645),translate 预测惰性却落在中段(4,869)。全部 11 个数据源上,没有任何几何量与加速的相关性超过 \|r\| = 0.35。 |
| **H5** | 至少加速现象本身是真的(哪怕无法解释) | **否证** | 预算对照,这次 seed 数足够可信。见 §4。 |

---

## 4. 决胜实验(Round 3)

设计与判读规则在**任何 run 启动之前**落盘于
[`reports/PREREG_ROUND3.md`](transfer-unit/reports/PREREG_ROUND3.md)。这是给怀疑者看的表。

**B = codegen · 4 条件 × 8 seeds · 评测集 n=48 · 主指标 = pass@1 曲线的 AUC**

| 条件 | 含义 | n | AUC | tt@0.7 (B token) | 总 token 口径 |
|---|---|---|---|---|---|
| `none` | 30k 目标 token,1 epoch | 8 | 0.613 | 11,871 | 1.28× |
| `b90k` | 90k 目标 token —— **预算对照** | 8 | **0.710** | 9,874 | 1.54× |
| `mh15` | 30k 目标 + 15k hard math | 7 | 0.646 | 7,250 | 1.40× |
| `pyf60` | 30k 目标 + 60k 错误恢复轨迹 | 8 | **0.719** | 5,681 | 0.89× |

**Mann–Whitney(AUC,双侧)**

| 比较 | p | 读法 |
|---|---|---|
| `pyf60` vs `none` | **0.015** | 辅助数据确实打赢欠训练基线 |
| `pyf60` vs `b90k` | **0.878** | **打不赢"多加自己的数据"** ← 定案 |
| `mh15` vs `b90k` | 0.336 | 同上 |
| `b90k` vs `none` | 0.083 | 预算效应本身是主要项 |

*总 token 口径 = 15,215 / (tt@0.7 × (1 + C/B)),其中 15,215 是 Round 1 `none` 的 tt@0.7 基线;
跨轮归一,仅供条件间相对比较。*

`tt@0.7` 一列正是早期叙事看起来那么漂亮的原因:**它只数目标 token,
藏掉了辅助数据买来的额外优化步**。总 token 口径下 `pyf60` 是 0.89×——不如什么都不做。

**状态**:31/32 run 完成(`mh15_s5` 未启动)。HANDOFF 写作时为 29/32(`b90k` 当时 n=6);
本文件已用补齐后的 `b90k` n=8 复算,**结论与所有 p 值判读均未改变**
(`b90k` AUC 0.717→0.710、tt@0.7 9,680→9,874、总 token 1.57×→1.54×;
`pyf60` vs `b90k` p 0.897→0.878)。

---

## 5. 通过了全部对照的结论

这些是**可以拿去写论文**的部分:

1. **同任务失败轨迹是毒药。** 把目标任务自己的失败轨迹混进蒸馏,准确率大约折半
   (0.33–0.56 vs 基线 0.72)——全项目最大、最可复现的内容效应。
   **跨任务的失败轨迹无害**。区别不在"是否出错",而在"**是否恢复**"。
2. **高剂量无关数据是轻度有害的,不是惰性的**(0.694 vs 基线 0.715,n=3)。
   "掺什么都行"被否定。
3. **欠训练是主导项。** 大部分看起来像数据配比魔法的东西,其实是 30k 预算太小;
   解法是加 epoch 或加数据,哪个便宜用哪个。
4. **RL 与 SFT 存在结构性差异。** 任何催化叙事都必须解释:为什么 on-policy RL
   **没有作用位点**——group-relative 优势把辅助数据与目标任务的学习信号隔离了。

---

## 6. 产生过假发现的陷阱

以下每一条都曾生成一个**看起来可发表**的结果,然后才被抓住。继续这项工作的人会再遇到它们。

| 陷阱 | 后果 | 修法 |
|---|---|---|
| **vLLM 采样 seed 碰撞** | 一个 `SamplingParams` seed 被整个 group 共用 → G 条 rollout 完全相同(82% 的 group 是同一条的 8 份拷贝)。制造了一个很有说服力的"实例级双峰"故事,并**作废了 v3–v6 全部 RL run** | 每个 request 独立 seed |
| **端点方差 ±0.2** | 小预算下单 seed 端点比较会在 seed 间**自我反转**排序 | 最少 3 seeds;优先用整条曲线的统计量(AUC)而非单个穿越点 |
| **loss 归一化混淆** | 用 batch 自己的梯度 token 数做分母,会让目标任务的有效学习率依赖于该条件保留了多少辅助数据——**正是被操纵的自变量** | 共享的常数分母 |
| **难度筛选的赢家诅咒** | k=8 采样筛 0/8 实例得到的"零信号"池约 1/3 是假阳性,池子会在十步内自燃 | 提高 k / 交叉验证 |
| **checkpoint 间距偏差** | 按优化步存 checkpoint,会让高辅助条件在**目标 token 轴上采样更密**,系统性地把 time-to-τ 压低——恰好偏向被测条件 | 按混合比例调 `save_every`,使 B-token 采样间隔相等 |
| **坐标轴选择本身是混淆** | "每目标 token 更快"可能只是"总共走了更多优化步" | 永远同时报告总 token 视图 |

---

## 7. 新接手的人该往哪推

否证是有范围的。以下是原始想法**仍可能成立**的方向:

1. **换一个受能力约束、而非受预算约束的目标任务。**
   我们的目标任务在小预算下就有 54% pass@1,"多给数据"永远是个替代解释。
   选一个模型**真正卡住**的目标,辅助数据才有额外目标数据做不到的事可做。
2. **换规模。** 已发表的跨域结果(math↔code,influence function,NeurIPS 2025)是
   7–8B + 真实语料。4B + 合成任务可能根本在"共享推理回路"存在的规模之下。
3. **换阶段:mid-training 而非 fine-tuning。** 这个想法最可信的版本是
   "**什么数据让模型之后更 RL-able**"——一个在表征仍可塑阶段的速率问题。我们没有任何 run 测过它。
4. **改推毒药结论。** 它是唯一通过全部对照的效应,量级大,且与常见实践冲突
   (rejection-sampling 流水线为了"多样性"保留失败轨迹)。它需要的是**第二个目标任务和一个机制**,不是更多 seed。
5. **若继续追速度,先修测量。** 每条件 10+ seeds、AUC 为主指标、报告总 token 轴、
   checkpoint 间距对齐、预测预注册。做不到这些只会再产出一张我们这样的表。

---

## 8. 实验清单

157 个 run 目录,曲线与日志全部在库。Checkpoint(~1.2 TB)**刻意未保留**。

| 阶段 | 前缀 | run 数 | 问题 | 结果 |
|---|---|---|---|---|
| RL 轨迹过滤 | `v7_*`, `hard2_*` | 12 | 被过滤的 rollout 是否承载跨任务加速? | 无效应;顺序课程是税 |
| SFT 剂量响应 | `sftd_*` | 14 | 掺料有用吗?什么剂量? | 终点上有——后被预算解释掉 |
| SFT 曲线 + 对照 | `sftc_*`, `sftc2_*` | 45 | 内容 vs 预算 vs 极性,逐 checkpoint | 毒药结论存活;催化不存活 |
| 速度 / math→coding | `mc_*` | 41 | 9 个辅助领域上的 time-to-τ | 表观加速很大,后被压平 |
| 决胜高 seed | `r3_*` | 31 | 有什么能打赢预算对照吗? | 没有(p = 0.88) |
| 冒烟 / 早期探索 | `smoke*`, `cat*`, `sftcat*` | 14 | 基建与早期试探 | — |

---

## 9. 文档时效性(重要)

`reports/` 下的文档是**按时间顺序的历史记录**,其中几份记载的是**后来被否证的立场**。
按下表阅读,不要把已死的理论当作当前结论:

| 文档 | 日期 | 状态 |
|---|---|---|
| `HANDOFF.html` | 08-29 | ✅ **当前** —— 最完整的实验室记录 |
| `PREREG_ROUND3.md` | 08-29 | ✅ **当前** —— 决胜实验的事前设计 |
| `LIT_REVIEW_speed.md` | 08-28 | ⚠️ 定位仍有效,但其所"占的空位"依赖 H4,而 H4 已死 |
| `SPEED_THEORY.md` | 08-28 | ❌ **记载 H4(秩补充假说)** —— 次日被 out-of-sample 检验否证 |
| `PREREG_ROUND2.md` | 08-29 | ❌ 其预测 1/5 命中,即 H4 的死亡证明 |
| `PREREGISTERED_PREDICTION.md` | 08-28 | ❌ 同上(第一轮判据) |
| `FINAL_REPORT.md` | 08-27 | ⚠️ 终点质量阶段;毒药与预算结论有效,"错误恢复是催化剂"部分已被 H3 推翻 |
| `PILOT_RESULTS.md` / `EXPERIMENT_BRIEF.md` / `pilot_design.md` | 08-25/26 | 📁 历史 —— RL 阶段,受 seed 碰撞 bug 影响 |
| `LIT_REVIEW_catalyst.md` | 08-27 | ✅ 文献定位,仍有效 |
| `T0_novelty.md` / `T2_T3_E0_acceptance.md` / `story_skeletons.md` | 08-25 | 📁 历史 —— 立项期材料 |

**建议阅读顺序**:本文件 → `HANDOFF.html` → `PREREG_ROUND3.md` → `FINAL_REPORT.md`。

---

## 10. 代码地图

| 路径 | 作用 |
|---|---|
| `tu/analysis/speed.py` | **因变量**:time-to-τ / AUC |
| `tu/offline/sft.py` | 混合实验:目标数据 + 辅助数据,周期存 checkpoint |
| `tu/offline/eval_ckpts.py` | 热载评测器 —— 一次引擎加载,约 2.5 分钟/checkpoint(让曲线网格可负担的关键基建) |
| `tu/offline/donate.py` | donor 数据集生成(`data/donor_*.jsonl`,未入库,可再生) |
| `tu/tasks/` | 11 个程序化可验证的合成任务(agentic / 推理 / 语言三类形态) |
| `tu/training/grpo.py`, `rollout.py` | RL 阶段:GRPO 与 rollout(H1 阶段使用) |
| `tu/tracing/` | trace 特征打标与 span 标注器 |
| `tu/analysis/catalysis.py`, `curves.py`, `final_figure.py` | 加速度量、曲线、出图 |
| `scripts/` | 实验编排(网格发起、恢复、评测 sweeper、SFT 流水线) |
| `configs/` | 31 份实验配置 |

---

## 11. 环境注意事项(否则会浪费一天)

- 栈锁定 CUDA 12:`vllm 0.19.1`,`torch 2.10+cu128`,`transformers 5.13`,`fla-core`。Hopper GPU,驱动 535。
- `fla-core` 会探测一个损坏的 TileLang 安装,并在 triton ≥ 3.4 的 Hopper 上拒绝
  gated-DeltaNet backward —— 两者都在 `scripts/*.sh` 里绕过(shim + monkeypatch 到纯 torch 路径)。
- 需要 `VLLM_ALLOW_INSECURE_SERIALIZATION=1` 才能把 callable 传给 `collective_rpc` 做权重同步;
  需要 `TORCH_BLAS_PREFER_CUBLASLT=1`,否则每次 bf16 加载都会崩。
- 共享 NFS home 上,NVML 符号链接的 workaround 必须**按 host 区分**,
  否则不同驱动版本的 worker 会互相覆盖对方的修复。
- **路径是绝对路径**:换机器前先 `grep -rl chobits-wx` 并重新指向。

---

## 12. 未入库的产物

| 路径 | 体积 | 说明 |
|---|---|---|
| `runs/` | ~1.5T | 训练 run 输出与 checkpoint(曲线/日志已随报告入库) |
| `models/` | ~13G | 基座模型权重 |
| `envs/` | ~4.9G | 虚拟环境 |
| `data/*.jsonl` | ~137M | 生成的 donor 数据集,由 `tu/offline/donate.py` 可再生 |
